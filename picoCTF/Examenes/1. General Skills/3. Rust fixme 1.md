#### Description

Have you heard of Rust? Fix the syntax errors in this Rust file to print the flag! Download the Rust code [here](https://challenge-files.picoctf.net/c_verbal_sleep/3f0e13f541928f420d9c8c96b06d4dbf7b2fa18b15adbd457108e8c80a1f5883/fixme1.tar.gz).

Al checar el codigo fuente de "main.rs" hay comentarios que nos guian a encontrar los errores, lo unico que se necesita es saber sintaxis muy basica de Rust.

En la siguiente línea hay que colocar punto y coma al final de la sentencia:

```
let key = String::from("CSUCKS") // How do we end statements in Rust?
```

Para imprimir una variable usando "println" se usa "{}"

```
   let decrypted_buffer = xrc.decrypt_vec(encrypted_buffer);
    println!(
        ":?", // How do we print out a variable in the println function? 
        String::from_utf8_lossy(&decrypted_buffer)
    );

```

Aqui no es "ret", es "return"

```
if res.is_err() {
        ret; // How do we return in rust?
    }
```